# 模型配置
model:
  name: "openbmb/MiniCPM-V-4.6"
  base_model: "openbmb/MiniCPM-V-4.6"
  use_quantized: true
  quantization: "int4"  # 通过 load_in_4bit=True 实现
  device: "cuda"
  torch_dtype: "auto"

# 推理配置
inference:
  max_new_tokens: 256
  temperature: 0.2
  do_sample: false
